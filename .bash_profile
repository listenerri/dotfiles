#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc



##################Costom Config###################

# auto startx after login
#[[ -z $DISPLAY && $XDG_VTNR -eq 1 || $XDG_VTNR -eq 2 ]] && exec startx
